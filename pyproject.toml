[tool.ruff]
line-length = 120

[tool.ruff.lint]
# Pragmatic default: pycodestyle errors/warnings, pyflakes, import sorting.
# E501 (line length) stays ignored per project convention.
select = ["E", "F", "W", "I"]
ignore = ["E501"]
